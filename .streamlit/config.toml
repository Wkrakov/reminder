[theme]
primary = "#CDF43D"                # кнопки, ссылки, ползунки — кислотно-зелёный
backgroundColor = "#1A1525"        # тёмный фон (почти чёрный с фиолетовым отливом)
secondaryBackgroundColor = "#2A2335" # чуть светлее для боковой панели и карточек
textColor = "#F0EFF4"              # светлый текст
